spring:
  datasource:
    url: jdbc:sqlserver://localhost;dataBase=BARBER;
    username: root
    password: root
  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.SQLServer2012Dialect
      hibernate.ddl-auto: update